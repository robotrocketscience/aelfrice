aelfrice
========

Bayesian memory that learns from feedback. Lock + correct + observe over many sessions
and the agent stops re-discovering things.

This repository is under active rebuild. v0.1.0 ships the storage foundation only.
